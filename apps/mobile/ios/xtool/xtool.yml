version: 1

# must match capacitor.config.ts appId
bundleID: org.scottylabs.quest
product: App

# merged over xtool's generated plist
infoPath: Info.plist

# copied to the .app root by basename, straight out of what cap sync writes
resources:
  - ../App/App/public
  - ../App/App/capacitor.config.json
