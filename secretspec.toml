[project]
name = "quest"
revision = "1.0"

[profiles.default]
MASTER_KEY = { description = "32-byte SDM master secret, hex-encoded (64 chars)", required = true }

[profiles.dev]
HOST = { description = "Backend bind address", default = "127.0.0.1" }
PORT = { description = "Backend listen port", default = "8080" }
